# Third-party attribution

Mandem is MIT licensed. The architecture standard adapts Nucleus guidance at the immutable commit
recorded in `docs/sources/doctrine-source-manifest.yaml`.

`git-native-issue` is an external GPL-2.0 executable pinned to v1.3.3. Mandem neither copies,
vendors, bundles, nor links it.

The repository-owned `track-git-native-issues` skill uses Mandem's work-ledger requirements and the
installed command's public interface. Its design also considered the GPL-2.0
`remenoscodes/claude-git-native-issue` plugin as a behavioral reference. Mandem does not copy or
bundle that plugin's code, hooks, or skill text.
