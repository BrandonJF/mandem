# Third-party attribution

Mandem is MIT licensed. The architecture standard adapts Nucleus guidance at the immutable commit recorded in `docs/sources/doctrine-source-manifest.yaml`. `git-native-issue` is an external GPL-2.0 executable pinned to v1.3.3; Mandem neither copies, vendors, bundles, nor links it.
